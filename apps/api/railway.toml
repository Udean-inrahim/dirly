[build]
builder = "nixpacks"
buildCommand = "npm install && npm run db:generate && npm run build"
startCommand = "npm run start -w @dirly/api"

[deploy]
healthcheckPath = "/api/health"
healthcheckInterval = 30
healthcheckTimeout = 5